# Sprite assets

The repository contains only runtime-ready WebP sprites in `assets/sprites/`.

Large white/black source plates and intermediate sheets are intentionally excluded from Git to keep clones and history small. Keep any future source-art workflow outside this repository or in Git LFS; commit only the final runtime sprites.
